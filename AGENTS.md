# 工作约定

快速穿刺，优先解决原问题。不要用边界、框架和附加功能代替可用结果。

直播前端、模型和动作调整先在本地模拟器中完成：

- 启动 `启动模拟器.ps1`，访问 `http://127.0.0.1:17874/sim`。
- 修改 `simulator/workspace/` 中的独立网页和模型副本，点击「刷新排练版本」验收。
- 开发与测试不能使用 `17870` 直播端口、真实弹幕接收器或正在运行的直播窗口。
- 不把试验改动直接写入直播使用的 `web/`。发布是后续单独步骤，按用户的发布指令执行。
- 用模拟弹幕、已有语音、打断和连续录像验证实际效果。模拟器不验证线上模型回答或上游 TTS。

模拟器首次启动才从 `web/` 复制副本，后续启动不会覆盖正在调试的版本。
