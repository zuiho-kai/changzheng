# 直播姬接入准备

本机已安装并成功打开哔哩哔哩直播姬 **7.64.0.10819**，启动入口 `G:\livehime\livehime.exe`。当前账号已登录，界面停在「开始直播」之前。没有对外开播。

已观察到素材/H5插件入口；安装包中有 `obs-browser.dll` 与 `win-capture-audio.dll`。这证明具备浏览器及音频采集组件，尚不等于完成实际素材导入验收。

## 已准备的画面

- 完整1920×1080背景与桌宠：[直播画面](http://127.0.0.1:17861/stage)
- 叠加在游戏上的透明桌宠：[透明画面](http://127.0.0.1:17861/overlay)
- 桌面控制：[小征](http://127.0.0.1:17861/)

直播姬里新建一个小征专用场景，添加浏览器/H5网页素材，填入上述一个画面地址，尺寸1920×1080。网页只接收公开直播场景的已播字幕，不显示私人聊天、记忆或任务内容。

画面网页本身不重复播音；声音从小征桌面进程输出。优先在直播姬选择应用音频采集并指定小征，避免同时采集同一声音两次。当前直播姬已有桌面音频来源；正式联调时需要核实混音选择和实际录制音轨，而不能只看音量条。

小征切到「直播预览」，现有弹幕测试入口可验证候选合并、过期丢弃和短回复。尚未连接真实B站弹幕，不能把该入口当成真实观众接入。

## 本次操作限制

直播姬可被截图读取，但两次本地素材/场景点击未出现预期界面变化。其可执行文件清单要求 `requireAdministrator`；当前控制通道没有完成管理员窗口输入。未修改权限设置，也未覆盖现有场景文件。素材导入、实际录制和正式开播仍待在可控制窗口里完成。

主人已明确授权标题改为「测试直播」并开播。此前操作工具因无法可靠识别浏览器 URL 而终止控制，标题修改和实际推流均未完成；当前阻塞是电脑控制能力，不是缺少开播授权。

本地 Live2D 已接入：在小征形象下方选择「Live2D · 日和」，stage / overlay 会同步切换。嘴型来自当前音频设备输出位置的 RMS，不读取麦克风；私人场景不向直播观察者发送嘴型。断开连接或超过250ms无更新时闭嘴。该页面不自行合成或播放声音。样例模型署名和使用条件见 web/models/NOTICE.md，开播场景介绍应保留其中的版权说明。

## 弹幕接入依据

B站官方 [OpenLive C#示例](https://github.com/bilibili-openplatform/OpenLive_CSharpDemo) 使用项目AppId、AccessKey和主播身份码建立互动长连接。当前未获得该应用配置，未自动注册平台应用或创建凭据。将来收到的弹幕应先进入本项目有界候选池，再由助手选题回应，不能直接逐条调用模型/TTS。
