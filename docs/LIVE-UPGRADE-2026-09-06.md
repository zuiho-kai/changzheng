# 直播体验更新 · 2026-09-06

当前 17870 已重启加载本轮代码，播音窗口也已重开，Codex 预览页已刷新。运行配置为 `Qwen/Qwen3.6-35B-A3B` + CosyVoice2 `diana`；B站接收器保持运行。

- 画面：奶油白与浅绿背景，人物居中偏左放大，右侧五条弹幕，下方显示实际已播字幕与本轮选中弹幕。
- 动作：移除六套固定姿势轮换，改为不规则换重心、接话探头、音量重音点头与短促弹跳，头身发梢依次跟随。保留真实音量口型与停止闭嘴。
- 对话：独立直播人设、句首表演标签、短句先合成与播放；风格提示仅用短句，避免长指令被语音服务念出。

## 实际验收

`artifacts/live-upgrade-loaded-check.json` 是当前实际 Electron 播音端完成的一条标记为“系统联调”的本地输入；不是新收到的B站观众弹幕。消息进入到开口 2.317 秒，其中模型首字 620 ms，首段 TTS 649 ms。完整播完，字幕随已播片段更新，最大口型 0.985、结束为0，页面错误为空；刷新观察页不丢失播音端。

隔离实例的最新三次真实云端联调分别约 2.94、7.97、2.87 秒开口，慢请求模型首字耗时6.04秒。保留在 `artifacts/live-upgrade-check.json`，不能把2秒样本当作稳定延迟保证。独立声音试验另出现过11.7秒TTS请求。

动作有同一音轨、原速26秒的前后对照：`artifacts/reactive-motion/before.mp4` 与 `after.mp4`。该对照使用旧音频，只用于评价动作；新版声音试听为 `artifacts/voice-review/audition-diana-tease.wav`。Diana是当前候选，音色自然度与调皮程度尚未获得用户听感验收。

本轮最后检查：语音与表演交付12项测试通过，前端脚本语法与diff空白检查通过；此前动作实录7项、动作契约6项和表情回归21项通过。没有据此宣称外网观众端音画已验收。

## Neko 借鉴结论

详见 [源码核查](NEKO-LIVE-MODE.md)。保留小征现有引擎，借少量弹幕选题、真实播放完成门控和同情绪动作变体。Neko直播原生语音依赖专用上游；其默认弹幕20秒窗口和20条数量池不适合照搬到当前低流量直播间。
